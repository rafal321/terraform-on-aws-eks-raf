kind: PodChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  namespace: default
  name: test2122
spec:
  selector:
    namespaces:
      - default
    labelSelectors:
      app: nginx
  mode: random-max-percent
  value: '60'
  action: pod-kill
  duration: 60s
  gracePeriod: 5